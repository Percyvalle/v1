# В репозитории всё хранится с LF, в рабочей копии на Windows git отдаёт CRLF.
# Без этого каждый файл при первом добавлении выдаёт предупреждение о замене
# перевода строки.
* text=auto

# Бинарное — не трогать переводы строк
*.png binary
*.jpg binary
*.webp binary
*.mp3 binary
*.db binary
